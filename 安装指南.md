# 🔧 安装问题修复指南

## 问题诊断

你的系统遇到了权限问题：
- ✅ **后端依赖已成功安装** (`server/node_modules`)
- ❌ **前端依赖安装失败** - `client/node_modules` 属于 root 用户

## 🚀 解决步骤

### 步骤 1: 删除 root 权限的文件

在你的终端执行（需要输入密码）：

```bash
cd /Users/xinguanliyuan/Desktop/indoor_chart/chiavow/client
sudo rm -rf node_modules package-lock.json
```

### 步骤 2: 重新安装前端依赖

```bash
npm install
```

### 步骤 3: 启动项目

**终端 1 - 启动后端：**
```bash
cd /Users/xinguanliyuan/Desktop/indoor_chart/chiavow/server
npm run dev
```

后端应该会成功启动在 http://localhost:3001

**终端 2 - 启动前端：**
```bash
cd /Users/xinguanliyuan/Desktop/indoor_chart/chiavow/client
npm run dev
```

前端应该会成功启动在 http://localhost:3000

### 步骤 4: 访问应用

打开浏览器访问：**http://localhost:3000**

---

## ✅ 已完成的修改

我已经修复了 canvas 库的问题：

1. ✅ 从 `package.json` 移除了 `canvas` 依赖
2. ✅ 修改了 `avatarGenerator.ts` 使用 SVG 代替 canvas
3. ✅ 更新了 Dockerfile 移除 canvas 系统依赖
4. ✅ **后端依赖已成功安装**

现在头像会生成为 SVG 格式，不再需要 canvas 库和系统依赖。

---

## 📋 快速检查清单

运行这些命令验证：

```bash
# 检查后端依赖（应该显示 node_modules 目录）
ls /Users/xinguanliyuan/Desktop/indoor_chart/chiavow/server/node_modules

# 检查前端依赖（执行步骤1-2后应该成功）
ls /Users/xinguanliyuan/Desktop/indoor_chart/chiavow/client/node_modules

# 检查是否可以启动后端
cd /Users/xinguanliyuan/Desktop/indoor_chart/chiavow/server
npm run dev
# 应该看到: "Server running on port 3001"

# 检查是否可以启动前端（新终端）
cd /Users/xinguanliyuan/Desktop/indoor_chart/chiavow/client
npm run dev
# 应该看到: "Local: http://localhost:3000"
```

---

## 🆘 如果还有问题

### 问题 A: 前端依赖安装仍然失败

尝试清理 npm 缓存：

```bash
npm cache clean --force
cd /Users/xinguanliyuan/Desktop/indoor_chart/chiavow/client
npm install
```

### 问题 B: ts-node-dev 命令找不到

后端依赖已经包含这个包了，应该不会有问题。如果还是报错：

```bash
cd /Users/xinguanliyuan/Desktop/indoor_chart/chiavow/server
npm install ts-node-dev --save-dev
```

### 问题 C: 端口已被占用

如果看到 "Port 3000 is already in use"：

```bash
# 查找并停止占用端口的进程
lsof -ti:3000 | xargs kill -9
lsof -ti:3001 | xargs kill -9
```

---

## 🎯 测试应用

一切正常后：

1. **登录**
   - 输入手机号: `+86 13800138000`
   - 点击 "Send Code"
   - 查看后端控制台的验证码（6位数字）
   - 输入验证码登录

2. **设置资料**
   - 填写用户名
   - 选择语言
   - 点击 Start（头像会自动生成为 SVG）

3. **创建订单**
   - 选择城市和日期
   - 添加多个行程
   - 设置旅行者数量
   - 提交订单

---

## 📞 需要帮助？

如果执行上述步骤后仍有问题，请提供：
1. 错误信息的完整输出
2. 运行的具体命令
3. 系统信息（macOS 版本，Node.js 版本）

---

**祝你运行成功！** 🎉
